[
  {
    "name": "trig-fastapi-route",
    "tags": ["trigger", "fastapi-service"],
    "arms": ["with"],
    "expect": ["fastapi-service"],
    "forbid": ["project-scaffolding"],
    "prompt": "Add GET /v1/authors/{author_id} to our FastAPI service. It should return the author or 404. Show me the route and the service method."
  },
  {
    "name": "trig-postgres-error",
    "tags": ["trigger", "postgres-database"],
    "arms": ["with"],
    "expect": ["postgres-database"],
    "forbid": [],
    "prompt": "I get this in my FastAPI app when serializing a book: sqlalchemy.exc.MissingGreenlet: greenlet_spawn has not been called; can't call await_only() here. The Book model has a relationship to Author. How do I fix it properly?"
  },
  {
    "name": "trig-alembic-casual",
    "tags": ["trigger", "postgres-database"],
    "arms": ["with"],
    "expect": ["postgres-database"],
    "forbid": [],
    "prompt": "alembic autogenerate produced an empty migration even though I added a new model, why?"
  },
  {
    "name": "trig-testing",
    "tags": ["trigger", "python-testing"],
    "arms": ["with"],
    "expect": ["python-testing"],
    "forbid": [],
    "prompt": "Write pytest tests for our POST /v1/authors endpoint (FastAPI + Postgres). Cover success and the duplicate-author 409 case."
  },
  {
    "name": "trig-agents",
    "tags": ["trigger", "ai-agents"],
    "arms": ["with"],
    "expect": ["ai-agents"],
    "forbid": [],
    "prompt": "Add a chatbot endpoint to our FastAPI service using pydantic-ai that can answer questions about our product catalog by calling the existing CatalogService."
  },
  {
    "name": "trig-tooling-vs-testing",
    "tags": ["trigger", "python-tooling", "near-miss"],
    "arms": ["with"],
    "expect": ["python-tooling"],
    "forbid": ["python-testing"],
    "prompt": "Set up the pytest and pytest-asyncio configuration in pyproject.toml for this project, and add ruff plus pre-commit hooks. We use uv."
  },
  {
    "name": "trig-makefile-ty",
    "tags": ["trigger", "python-tooling"],
    "arms": ["with"],
    "expect": ["python-tooling"],
    "forbid": ["python-testing"],
    "prompt": "How do I add a Makefile target that runs ty type checking through uv?"
  },
  {
    "name": "trig-scaffold",
    "tags": ["trigger", "project-scaffolding"],
    "arms": ["with"],
    "cwd": "empty",
    "max_turns": 12,
    "expect": ["project-scaffolding"],
    "forbid": [],
    "prompt": "Create a brand-new FastAPI app from scratch for managing a library of books with authors. It needs Postgres, tests and CI from day one. Do not run anything yet, just tell me exactly what you would do and which commands you would run."
  },
  {
    "name": "trig-nearmiss-style",
    "tags": ["trigger", "python-code-style", "near-miss"],
    "arms": ["with"],
    "expect": ["python-code-style"],
    "forbid": ["fastapi-service", "postgres-database"],
    "prompt": "Refactor this function to be more readable, plain Python, no framework involved:\n\ndef f(d):\n    r = []\n    for k in d:\n        if d[k] is not None:\n            if d[k] > 0:\n                r.append((k, d[k]))\n    return r"
  },
  {
    "name": "trig-agent-mock-vs-testing",
    "tags": ["trigger", "ai-agents", "near-miss"],
    "arms": ["with"],
    "expect": ["ai-agents"],
    "forbid": ["python-testing"],
    "prompt": "In our pydantic-ai tests I want the agent to return a fixed structured answer without calling OpenAI. How should I mock the model?"
  },
  {
    "name": "beh-domain-exception",
    "tags": ["behavior", "fastapi-service"],
    "arms": ["with", "without"],
    "expect": ["fastapi-service"],
    "forbid": [],
    "prompt": "Add GET /v1/authors/{author_id} to this FastAPI service: an AuthorService.get_author_by_id(author_id) that returns the author or produces a 404 when missing, plus the route. Write the files.",
    "must": ["NotFoundError|class \\w*NotFound\\w*\\(", "exception_handlers=|add_exception_handler\\(|@app\\.exception_handler", "Depends\\("],
    "must_not": ["def get_author_by_id[\\s\\S]{0,400}raise HTTPException"]
  },
  {
    "name": "beh-loading",
    "tags": ["behavior", "postgres-database"],
    "arms": ["with", "without"],
    "expect": ["postgres-database"],
    "forbid": [],
    "prompt": "Write the SQLAlchemy 2.0 async service method list_books(filters, pagination_params) for this FastAPI service. BookModel has a many-to-one relationship to AuthorModel and a many-to-many to TagModel, and the Book response schema includes author and tags. Use fastapi_pagination. Write the file.",
    "must": ["selectinload\\(BookModel\\.author\\)", "selectinload\\(BookModel\\.tags\\)", "apaginate"],
    "must_not": ["joinedload\\(BookModel\\.tags\\)", "joinedload\\(BookModel\\.author\\)", "\\.unique\\(\\)"]
  },
  {
    "name": "beh-model",
    "tags": ["behavior", "postgres-database"],
    "arms": ["with", "without"],
    "expect": ["postgres-database"],
    "forbid": [],
    "prompt": "Define the SQLAlchemy models for Author (first_name, last_name, birthday, unique on the three) and Book (title, author_id FK, one-to-many from Author; deleting an author deletes its books), with created_at / updated_at timestamps. Write the files.",
    "must": ["Mapped\\[", "mapped_column\\(", "lazy='raise'|lazy=\"raise\"", "UniqueConstraint\\([\\s\\S]{0,200}name=", "String\\(\\d+\\)", "ondelete='CASCADE'|ondelete=\"CASCADE\"", "DateTime\\(timezone=True\\)"],
    "must_not": ["= Column\\(", "lazy='select'|lazy=\"select\"|lazy='selectin'|lazy=\"selectin\""]
  },
  {
    "name": "beh-no-utils",
    "tags": ["behavior", "python-code-style"],
    "arms": ["with", "without"],
    "expect": ["python-code-style"],
    "forbid": [],
    "prompt": "Three domains in this app each need a helper that slugifies a title string. Where should this function live in the app package and how should it be written? Write the file.",
    "must": ["def slugify\\w*\\([^)]*: str\\) -> str"],
    "must_not": ["utils\\.py", "helpers\\.py", "common\\.py", "shared\\.py", "from app\\.\\w+\\.utils|from app\\.utils"]
  },
  {
    "name": "beh-tests",
    "tags": ["behavior", "python-testing"],
    "arms": ["with", "without"],
    "expect": ["python-testing"],
    "forbid": [],
    "prompt": "Write pytest tests for POST /v1/authors in this FastAPI + Postgres service: success, and a 409 for a duplicate author. Assume the endpoint and AuthorCreate schema exist. Write the test file and any factories/fixtures you need.",
    "must": ["ModelFactory\\[", "httpx2", "AsyncClient\\(", "def test_success", "def test_fail_\\w+", "model_dump\\(mode='json'\\)|model_dump\\(mode=\"json\"\\)"],
    "must_not": ["TestClient\\(", "from httpx import", "sqlite", "mock\\w*\\(\\s*AuthorService|patch\\([^)]*AuthorService", "__check_model__ = False", "factory_use_construct=False"]
  },
  {
    "name": "beh-tooling",
    "tags": ["behavior", "python-tooling"],
    "arms": ["with", "without"],
    "expect": ["python-tooling"],
    "forbid": [],
    "prompt": "Add python-json-logger as a runtime dependency and set up ruff plus pre-commit for this project including the standard hygiene hooks (we use uv). Write the config files and tell me the commands.",
    "must": ["uv add python-json-logger", "repo: local", "uv run ruff", "line-length = 120", "quote-style = \"single\"|quote-style = 'single'", "rev: v\\d"],
    "must_not": ["pip install", "astral-sh/ruff-pre-commit", "\\[tool\\.black\\]", "\\[tool\\.isort\\]"]
  },
  {
    "name": "beh-agent",
    "tags": ["behavior", "ai-agents"],
    "arms": ["with", "without"],
    "expect": ["ai-agents"],
    "forbid": [],
    "prompt": "Add a chatbot endpoint to this FastAPI service using pydantic-ai that answers questions about our book catalog by calling an existing BookService.list_books(filters). Write the files.",
    "must": ["Agent\\[", "deps_type=", "RunContext\\[", "@\\w+\\.tool\\b", "frozen=True", "instructions="],
    "must_not": ["client\\.chat\\.completions", "openai\\.OpenAI\\(", "retries=0", "system_prompt="]
  },
  {
    "name": "beh-query-model",
    "tags": ["behavior", "fastapi-service"],
    "arms": ["with", "without"],
    "expect": ["fastapi-service"],
    "forbid": [],
    "prompt": "Add GET /v1/books to this FastAPI service. Filters are ids (a list of ints), title and author_id; the route also takes a BookListSorting model and fastapi_pagination Params. Write the route and the filter and sorting schemas.",
    "must": ["Annotated\\[\\w*Filter\\w*, Query\\(", "Annotated\\[Params, Depends\\(\\)\\]", "ids: list\\[int\\] \\| None"],
    "must_not": ["Annotated\\[\\w*Filter\\w*, Depends\\(", "Annotated\\[\\w*Sorting\\w*, Query\\("]
  },
  {
    "name": "beh-cascade",
    "tags": ["behavior", "postgres-database"],
    "arms": ["with", "without"],
    "expect": ["postgres-database"],
    "forbid": [],
    "prompt": "Deleting an author must delete that author's books. Update the SQLAlchemy models and write AuthorService.delete_author_by_id as a Core delete statement. Write the files.",
    "must": ["ondelete='CASCADE'|ondelete=\"CASCADE\"", "passive_deletes=True", "cascade='all, delete-orphan'|cascade=\"all, delete-orphan\"", "delete\\(AuthorModel\\)"],
    "must_not": ["_session\\.delete\\(", "self\\._session\\.commit\\(\\)"]
  },
  {
    "name": "beh-settings-override",
    "tags": ["behavior", "python-testing"],
    "arms": ["with", "without"],
    "expect": ["python-testing"],
    "forbid": [],
    "prompt": "Write a test for GET /health/live that asserts the version field is 9.9.9 by pointing that one test at a different PROJECT_VERSION. The endpoint reads Settings through the get_settings dependency. Write the test file and any helper it needs.",
    "must": ["temporary_override\\(", "model_copy\\(update=", "get_settings"],
    "must_not": ["monkeypatch\\.setenv", "get_settings\\.cache_clear\\(\\)", "os\\.environ\\['PROJECT_VERSION'\\]|os\\.environ\\[\"PROJECT_VERSION\"\\]"]
  },
  {
    "name": "beh-uv-remove",
    "tags": ["behavior", "python-tooling"],
    "arms": ["with", "without"],
    "expect": ["python-tooling"],
    "forbid": [],
    "prompt": "We deleted the last import of requests from the app. Remove the dependency from the project and give me the exact commands to run afterwards.",
    "must": ["uv remove requests", "uv run ruff (?:format|check)|make lint"],
    "must_not": ["pip (?:uninstall|install)", "uv sync(?! --(?:locked|frozen))"]
  },
  {
    "name": "beh-agent-tools",
    "tags": ["behavior", "ai-agents"],
    "arms": ["with", "without"],
    "expect": ["ai-agents"],
    "forbid": [],
    "prompt": "Our pydantic-ai catalog assistant needs a list_items tool taking filters and a limit (1-100, default 20), and the default model should fail over to the fast model when the primary provider errors. Write the agent builder, the tool input schema and the model registry.",
    "must": ["@\\w+\\.tool\\b", "@\\w+\\.tool\\b[\\s\\S]{0,300}\"\"\"", "Field\\([^)]*description=", "ge=1,\\s*le=100|le=100,\\s*ge=1", "FallbackModel\\("],
    "must_not": ["system_prompt=", "retries=0", "isinstance\\(model,"]
  },
  {
    "name": "beh-scaffold-command",
    "note": "The --checkout SHA is duplicated from skills/project-scaffolding/SKILL.md; bump both together.",
    "tags": ["behavior", "project-scaffolding"],
    "arms": ["with", "without"],
    "cwd": "empty",
    "max_turns": 12,
    "expect": ["project-scaffolding"],
    "forbid": [],
    "prompt": "Set up a brand-new FastAPI service that stores orders in Postgres, right here in this empty directory. Do not run anything yet, just write out the exact command you would run.",
    "must": ["cookiecutter https://github\\.com/DenysMoskalenko/BoilerplateBuilder", "--checkout 492fc9b1e752a761e6a65182626502b8569e8143", "project_type=[\"']?fastapi_db\\b", "project_description=", "author_name=", "author_email=", "--no-input"],
    "must_not": ["use_pre_commit", "project_type=[\"']?fastapi_db_agent"]
  },
  {
    "name": "trig-nearmiss-container",
    "tags": ["trigger", "postgres-database", "near-miss"],
    "arms": ["with"],
    "expect": ["postgres-database"],
    "forbid": ["python-testing"],
    "prompt": "Our integration tests need a real Postgres. Set up the session-scoped testcontainers fixture and make the app read that container's DATABASE_URL."
  },
  {
    "name": "trig-nearmiss-schemas",
    "tags": ["trigger", "fastapi-service", "near-miss"],
    "arms": ["with"],
    "expect": ["fastapi-service"],
    "forbid": ["postgres-database"],
    "prompt": "Design the request and response schemas for our reviews endpoint: create, patch, response and the list filters. No database work yet, just the Pydantic models."
  },
  {
    "name": "trig-nearmiss-helper-placement",
    "tags": ["trigger", "python-code-style", "near-miss"],
    "arms": ["with"],
    "expect": ["python-code-style"],
    "forbid": ["fastapi-service"],
    "prompt": "Three of our domains need the same slugify function. Where should it live in the app package, and what should the signature look like?"
  },
  {
    "name": "trig-nearmiss-coverage",
    "tags": ["trigger", "python-tooling", "near-miss"],
    "arms": ["with"],
    "expect": ["python-tooling"],
    "forbid": ["python-testing"],
    "prompt": "Add pytest-cov and wire the 90% coverage gate into pyproject.toml and the CI job."
  },
  {
    "name": "trig-nearmiss-provider-error",
    "tags": ["trigger", "ai-agents", "near-miss"],
    "arms": ["with"],
    "expect": ["ai-agents"],
    "forbid": ["fastapi-service"],
    "prompt": "Our assistant endpoint answers 500 whenever the model provider rate-limits us. What status should it return instead, and where do I map that?"
  }
]
